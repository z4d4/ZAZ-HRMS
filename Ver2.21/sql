CREATE TABLE IF NOT EXISTS "Leave Applications" (
    Leave_ID INTEGER PRIMARY KEY AUTOINCREMENT,
    User_ID INTEGER,
    Start_Date DATE,
    End_Date DATE,
    Status TEXT,
    Reason TEXT,
    FOREIGN KEY(User_ID) REFERENCES "Employee Information"(User_ID)
);
